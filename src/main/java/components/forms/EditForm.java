package components.forms;

import org.openqa.selenium.WebDriver;


public class EditForm extends BaseForm {

    public EditForm(WebDriver driver, String label) {
        super(driver, label);
    }
}
