package components.forms;

import org.openqa.selenium.WebDriver;


public class CreateForm extends BaseForm {

    public CreateForm(WebDriver driver, String label) {
        super(driver, label);
    }
}